// const integer = try std.fmt.parseInt(i32, foo, 10);

pub fn roman_to_integer() void {}

test "A string is returned" {
    const message = "John Doe";
    const a = greeting(message);
    try expect(a == "Hello World! John Doe");
}
